apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "whizard-agent-proxy.fullname" . }}-test-connection"
  labels:
    {{- include "whizard-agent-proxy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "whizard-agent-proxy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
